# coding=utf-8
# --------------------------------------------------------------------------
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------

from msrest.service_client import SDKClient
from msrest import Configuration, Serializer, Deserializer
from .version import VERSION
from msrest.exceptions import HttpOperationError
from .operations.session_operations import SessionOperations
from . import models


class SimulatorAPIConfiguration(Configuration):
    """Configuration for SimulatorAPI
    Note that all parameters used to create this instance are saved as instance
    attributes.

    :param str base_url: Service URL
    """

    def __init__(
            self, base_url=None):

        if not base_url:
            base_url = 'http://localhost'

        super(SimulatorAPIConfiguration, self).__init__(base_url)

        self.add_user_agent('Bonsai.SimulatorApi/{}'.format(VERSION))


class SimulatorAPI(SDKClient):
    """This API allows simulators to provide states and receive commands from the platform.

    :ivar config: Configuration for client.
    :vartype config: SimulatorAPIConfiguration

    :ivar session: Session operations
    :vartype session: bonsai.simulatorapi.operations.SessionOperations

    :param str base_url: Service URL
    """

    def __init__(
            self, base_url=None):

        self.config = SimulatorAPIConfiguration(base_url)
        super(SimulatorAPI, self).__init__(None, self.config)

        client_models = {k: v for k, v in models.__dict__.items() if isinstance(v, type)}
        self.api_version = 'v1'
        self._serialize = Serializer(client_models)
        self._deserialize = Deserializer(client_models)

        self.session = SessionOperations(
            self._client, self.config, self._serialize, self._deserialize)
