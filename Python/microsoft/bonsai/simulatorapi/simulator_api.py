# coding=utf-8
# --------------------------------------------------------------------------
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is
# regenerated.
# --------------------------------------------------------------------------

from msrest.service_client import SDKClient
from msrest import Configuration, Serializer, Deserializer
from .version import VERSION
from msrest.exceptions import HttpOperationError
from .operations.gateway_operations import GatewayOperations
from .operations.simulator_notification_operations import SimulatorNotificationOperations
from .operations.simulators_operations import SimulatorsOperations
from .operations.simulator_session_operations import SimulatorSessionOperations
from . import models


class SimulatorAPIConfiguration(Configuration):
    """Configuration for SimulatorAPI
    Note that all parameters used to create this instance are saved as instance
    attributes.

    :param str base_url: Service URL
    """

    def __init__(
            self, base_url=None):

        if not base_url:
            base_url = 'https://api.bons.ai'

        super(SimulatorAPIConfiguration, self).__init__(base_url)

        self.add_user_agent('simulatorapi/{}'.format(VERSION))


class SimulatorAPI(SDKClient):
    """This API allows simulators to provide states and receive commands from the platform.

    :ivar config: Configuration for client.
    :vartype config: SimulatorAPIConfiguration

    :ivar gateway: Gateway operations
    :vartype gateway: microsoft.bonsai.simulatorapi.operations.GatewayOperations
    :ivar simulator_notification: SimulatorNotification operations
    :vartype simulator_notification: microsoft.bonsai.simulatorapi.operations.SimulatorNotificationOperations
    :ivar simulators: Simulators operations
    :vartype simulators: microsoft.bonsai.simulatorapi.operations.SimulatorsOperations
    :ivar simulator_session: SimulatorSession operations
    :vartype simulator_session: microsoft.bonsai.simulatorapi.operations.SimulatorSessionOperations

    :param str base_url: Service URL
    """

    def __init__(
            self, base_url=None):

        self.config = SimulatorAPIConfiguration(base_url)
        super(SimulatorAPI, self).__init__(None, self.config)

        client_models = {k: v for k, v in models.__dict__.items() if isinstance(v, type)}
        self.api_version = 'v2'
        self._serialize = Serializer(client_models)
        self._deserialize = Deserializer(client_models)

        self.gateway = GatewayOperations(
            self._client, self.config, self._serialize, self._deserialize)
        self.simulator_notification = SimulatorNotificationOperations(
            self._client, self.config, self._serialize, self._deserialize)
        self.simulators = SimulatorsOperations(
            self._client, self.config, self._serialize, self._deserialize)
        self.simulator_session = SimulatorSessionOperations(
            self._client, self.config, self._serialize, self._deserialize)
